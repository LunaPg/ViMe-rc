

'use strict';

const _ = require('lodash');
const moment = require('moment');
const Promise = require('bluebird');

module.exports = function(dataContentManager) {

const model = {
  tableName: 'Strongbox';
};

model.save = function() {};

model.findById = function(){};

model.delete = function() {};

model.update = function(id){};

return model
};
